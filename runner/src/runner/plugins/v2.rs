//! High-level V2 plugin trait. Re-exported from `jobworkerp-plugin-abi`
//! so both the host runner and downstream plugin crates resolve
//! identical definitions.

pub use jobworkerp_plugin_abi::v2::*;
