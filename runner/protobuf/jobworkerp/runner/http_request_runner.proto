syntax = "proto3";
package jobworkerp.runner;

// # HTTP Request Runner Settings
// Configuration for the HTTP request runner component
message HttpRequestRunnerSettings {
    // # Base URL
    // The base URL to which request paths will be appended
    string base_url = 1;
}
