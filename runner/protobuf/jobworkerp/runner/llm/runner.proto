syntax = "proto3";
package jobworkerp.runner.llm;

// settings for model initialization
message LLMRunnerSettings {
  // ollama model specific settings
  message OllamaRunnerSettings {
    // base_url (default: http://localhost:11434)
    optional string base_url = 1;
    // model name for runner default
    string model = 2;
    // default system prompt before the user prompt
    optional string system_prompt = 3;
    // pull the model from the server (set false if you want to use manually
    // loaded model)
    optional bool pull_model = 4;
  }

  // ollama model specific settings
  // NEED to SET env for auth token of the model
  // (OPENAI_API_KEY, ANTHROPIC_API_KEY, COHERE_API_KEY, GEMINI_API_KEY,
  // GROQ_API_KEY, XAI_API_KEY, DEEPSEEK_API_KEY)
  message GenaiRunnerSettings {
    // # model name
    // use api depending on the model name (of openai, anthropic, etc.)
    string model = 1;
    // default system prompt before the user prompt
    optional string system_prompt = 2;
    // # base url
    // base url to use for the api (optional, not set usually)
    optional string base_url = 3;
  }

  // Hierarchical chunking parameters shared by settings default and per-job
  // override. Defined here (nested) so embedding_args.proto references the
  // single canonical type as LLMRunnerSettings.ChunkingConfig.
  message ChunkingConfig {
    // Upper bound on tokens per chunk. Must be > 0.
    optional uint32 max_chunk_tokens = 1;
    // Lower bound used when merging small adjacent chunks. Must be <
    // max_chunk_tokens.
    optional uint32 min_chunk_tokens = 2;
    // Token-length estimation strategy. Default: CHARACTER_ESTIMATION.
    optional TokenEstimation token_estimation = 3;
  }

  enum TokenEstimation {
    TOKEN_ESTIMATION_UNSPECIFIED = 0;
    // ~4 chars/token, no tokenizer required (Phase 1)
    CHARACTER_ESTIMATION = 1;
    // OpenAI cl100k_base via tiktoken-rs (Phase 2)
    TIKTOKEN = 2;
    // HuggingFace tokenizer (Phase 3, Ollama)
    HF_TOKENIZER = 3;
  }

  // Seal the removed `local = 3` tag to prevent wire-compat issues from tag
  // reuse (an old payload's bytes written as `local` must not be misdecoded).
  reserved 3;
  reserved "local";

  // runner settings
  oneof settings {
    OllamaRunnerSettings ollama = 1;
    GenaiRunnerSettings genai = 2;
    // local = 3 was removed (now provided via plugin_runner_mistral)
  }

  // Default chunking config for the `embedding` method. Per-job override is
  // possible via LLMEmbeddingArgs.chunking. When unset, defaults are derived
  // from the model's context length hint or a safe default. Uses a new unused
  // tag (10) because tag 3 is reserved above.
  optional ChunkingConfig embedding_chunking = 10;
}